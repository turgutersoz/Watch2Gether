{
  "name": "react-watch-together",
  "version": "1.0.0",
  "description": "Watch2Gether benzeri senkronize video izleme platformu",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "postinstall": "cd client && npm install || true && cd ../server && npm install || true",
    "start": "cd server && npm start"
  },
  "keywords": ["watch-together", "video-sync", "socket.io"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

